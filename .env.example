# Copy this file to .env and adjust values for your local setup.
# None of these variables are required to run the app.

# Override the SQLite database file location (default: Electron userData dir)
# HITRO_DB_PATH=/path/to/hitro.db

# Open Chromium DevTools automatically on launch (1 = open)
# NEXUS_DEV_TOOLS=0
